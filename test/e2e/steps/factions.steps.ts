import { Then, When } from '@wdio/cucumber-framework';

When('I create a new faction', async function () {
    return 'pending';
});

Then('a new faction is created', async function () {
    return 'pending';
});

Then('the faction is active', async function () {
    return 'pending';
});
